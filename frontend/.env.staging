# Placeholder. Replace with the Railway staging API origin before first staging deploy.
# SPA origin will be https://preview.smartposting.ca (HOST_DOMAIN on the API).
VITE_API_BASE_URL=https://api.preview.smartposting.ca/
VITE_VERIFY_EMAIL=false
