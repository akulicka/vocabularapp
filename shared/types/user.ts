// Shared types for user-related DTOs
// Extracted from backend/src/types/user.ts (Express-specific types excluded)

// Note: Express-specific types like AuthenticatedRequest, FileUploadRequest, etc.
// are kept in the backend as they're server-side only

// Export empty object to make this a valid module
export {}
